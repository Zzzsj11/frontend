# dev01「测试」目录：视频质量与并发测试计划（历史归档）

状态：本文件保留 2026-09-21 的测试约束与原始提案，不是当前可执行计划或新的付费授权。PPIO 与 BFL（历史 Flux 3 Video）已从 MV 与 model-service 移除支持，相关准入、余额检查与补测流程作废；历史工单、费用和媒体保留，不重发生成。HappyHorse、Vidu、Veo 的原暂停决定不变，Kling 等其他模型不因本次退役被删除或获准启用。

## 当时已确认的执行修订（仅供追溯）

- Grok、Gemini 均保持 5 镜头，只调整生成时长为每镜 10 秒，总时长 50 秒。原始提示词、人物、镜头数量与其他配置不动，不重生成大纲或提示词。
- 全部视频选择 720 档。H3 的供应商原生 768P 映射会如实记录，不擅自升到 1080。
- 每镜头仅生成一次，失败不重生成。已有视频工单的镜头不重复提交，提交结果不明确时先对账。
- 当时排除新增对照视频和重复压力波次，计划使用 7×5=35 个唯一镜头，先每模型一个冒烟，剩余最多 28 个混合提交；暂停的三个模型不动。此数量包含现已退役渠道，不能沿用为新批次。
- 当时预计总时长 400 秒、原估价约 ¥273.82，不含供应商额外费用；这是历史预算，不是当前余额、价格或实际支出。
- 该小样本只能观察实际达到的并发、排队与完成表现，不能宣称系统或供应商极限，也不提供稳定 P95 结论。
- 下文 255 条及高并发扩展、重建大纲、测试副本安排均为当时未获准执行的原始提案。保留数量用于追溯，退役后不得按原提案执行；新批次须另行核验保留模型的能力、预算与授权。

## 1. 当时范围与现状

项目：`project-1f284f8d3fbb480ba18ec2f087282e60`，用户 dev01。盘点时有 10 个子项目、50 个镜头，大纲及提示词已就绪，尚未生成图片或视频。每项目原配置 5 镜头、每镜头 12 秒、总计 60 秒、720p、16:9；2 个空镜、3 个人物镜头。

下表仅保留未退役模型的历史安排，不代表当前准入；Grok/Gemini 的 6×10 秒原提案以文首 5×10 秒历史修订为准。

|模型|子项目时间后缀|当时声明时长|历史处理方案|
|---|---|---|---|
|Wan 3.0|10-13-48|4–15 秒|保留 5×12 秒，纳入|
|Kling v3|10-15-06|4–15 秒|保留 5×12 秒，纳入|
|MiniMax H3 / 英和|10-16-55|4–15 秒|保留 5×12 秒，纳入|
|Grok Video 1.5|10-17-30|6–10 秒|先调整为 6×10 秒，测试渠道条件准入|
|Gemini Omni Flash|10-19-10|4–10 秒|先调整为 6×10 秒，重点验证实际时长|
|HappyHorse|10-15-37|待用户核对|暂停，不生成、不进入并发批次|
|Vidu|10-18-11|待用户核对|暂停，不生成、不进入并发批次|
|Veo|10-18-38|待用户核对|暂停，不生成、不进入并发批次|

历史定位：Seedance 2.0 / PPIO 子项目后缀 `10-16-11`、BFL Flux 3 Video 后缀 `10-19-39` 仅供查找旧记录，已从测试准入表移除，不补测、不换渠道重发。Grok 的余额、额度、权限与模式仍须在另行获准的新批次中核验，不足时记录“未具备测试条件”，不算生成失败。

## 2. 原提案的前置修复：大纲按模型规划时长

现状：大纲通过全局 4–15 秒规则分配时长，到视频提交时才校验模型上下限。这会产生“大纲合法、模型不接受”的任务。

修复采用统一的模型时长规划函数，从注册表读取当前模型/模式的上下限、离散时长选项及步长。创建大纲前完成可行性检查；无解时返回可执行的镜头数/总时长组合，不调用大纲模型，也不生成注定失败的分镜。不能只对最终视频请求截断时长，否则大纲、提示词和成片时间会不一致。

- 优先保留用户总时长。原提案建议 Grok、Gemini 从 5×12 秒改为 6×10 秒；空镜仍保留 2 个，人物镜头调整为 4 个。若用户锁定 5 镜头，则明确提示总时长最多 50 秒，不能静默缩短。
- 自动调整必须在大纲提交结果中展示实际镜头数、各镜头秒数和总时长。锁定的约束相互冲突时，明确提示冲突。
- 同步大纲配置、故事规划、镜头 planned_duration、shot_options.duration 和提示词中的时间安排；原提案中的重建大纲与提示词未获准执行，以文首历史修订为准。
- 切换模型、修改镜头数量/总时长、重新生成大纲、单镜头编辑时复用相同校验；后端视频提交保留最后一道校验。
- 大纲只使用整个请求链路均支持的能力交集；单次样本落在合法范围内，不代表系统已覆盖供应商声明的全部边界。
- 验证连续区间、离散选项、无法整除、边界、无解、切换模型以及旧任务兼容。保证秒数总和准确，不因先分配小数再逐镜头取整造成总时长偏差。

交付检查：相关后端单测、前端单测、Playwright 大纲创建/模型切换/批量生成旅程；提交前 preflight-lite，发布前 preflight。检查通过不构成新的真实生成授权。

## 3. 准备与请求规范

1. 保存原始任务快照，在 dev01 范围内创建带批次标识的测试副本，保留原来的任务和结果；按业务 API 操作，不直接改生产数据库。
2. 使用 dev01 的有效授权会话。通过系统真实批量生成接口、队列、worker、供应商适配器、TOS 和结果回写完成全链路，不能绕过系统直接请求供应商。
3. 每个请求携带 `X-Agent-Name: code-agent`、本轮唯一 `X-Agent-Run-Id`、`X-Test-Run-Id`。执行前用最小冒烟验证工单和费用记录均能追踪到这些标识。
4. 固化模型 ID、渠道、实际请求参数、原始提示词及哈希、参考图原文件公网 URL、角色 ID、任务/镜头/工单 ID、供应商任务 ID。对外报告隐藏密钥和签名凭据。
5. 参考图使用原图，不使用缩略图；检查可访问性、媒体类型、尺寸、引用数量、角色资产状态及供应商使用参考图的实际模式。
6. 质量用例覆盖空镜、单人、现有双人镜头。某模型不支持对应参考方式时，该用例明确跳过；不能悄悄丢弃一张角色图后当成通过。
7. 统一比较基线采用 720p、16:9、10 秒；H3 的实际 768P 映射单独记录。音频不作为所有模型共有的硬性要求；支持音频的模型在质量样本中另外检查，Grok 无原生音频不判失败。
8. 检查日额度、供应商余额、在途任务、普通用户负载。批量接口最多 200 项，负载发生器分批提交；大批排队数量不等于真实并发。

## 4. 原提案的分阶段数量（未执行，不再适用）

以下仅解释历史预算的组成，不保留为操作流程。其中 Seedance 指已退役的 PPIO 子项目；全部阶段数量均为退役前方案，不能用于新任务准入或排队配置。

|原提案阶段|历史设计|原计划视频任务数|当时拟定条件|
|---|---|---:|---|
|A 冒烟与统一质量样本|7 模型各生成一个空镜和一个单人镜头，均 10 秒；每模型先 1 个完成，再启动第 2 个|14|请求合法、参考生效、真实时长/尺寸、归因/入库全部正常|
|B 现有业务样本|5 模型各 5×12 秒，Grok/Gemini 各 6×10 秒；模型内串行或低并发运行|37|业务链路完整，失败原因已归类；不把未通过用例投入压力测试|
|C 单模型并发阶梯|Wan、Kling、Seedance、H3、Gemini 分别测试并发 2、4、8，每档两波，每条 6 秒；各模型独立执行以便归因|140|每档达标再升档；失败仅停对应模型/共享渠道|
|D 系统混合并发|目标在途 32：Wan 8、Kling 8、Seedance 8、H3 4、Gemini 4；两波，每条 6 秒|64|系统与供应商指标稳定，结果/费用均正确|
|合计|全体模型满足准入条件时|255|总输出计划 1,784 秒|

阶段 A 的统一提示词才用于横向比较。阶段 B 的原有提示词不同，用于检验各自业务场景，不直接据此排名模型。阶段 C/D 使用固定的、经 A/B 验证的简单空镜/单人场景，避免内容差异主导耗时；每个重复样本仍需独立工单，不能被幂等或缓存合并。

当时 Grok 并发池为 1，原提案拟观察排队、串行释放和失败后是否堵塞后续任务；该设计不代表测得供应商并发上限。已退役渠道不再安排排队或压力测试。

两波短突发测试只能初筛。若要把某并发档认定为“稳定工作并发”，额外安排该档至少 10 分钟、至少 3 个完整周转周期的持续补位测试，取两者较长；追加任务和费用须另行授权，历史 255 条方案不包含这些任务。

## 5. 历史系统配置与容量结论边界

当时线上读取：视频 worker 总并发 170；用户视频并发上限 200；视频结果处理并发 20。Wan/Kling 共用英和 generation 池 200，H3 独立池 200，Gemini 所在 yseeai 池 20，Grok 测试池为 1。已剔除退役渠道的执行池配置；保留渠道当时也可能与正常业务共用池，这些数值不代表当前部署。

这些是本系统配置，不是供应商承诺，也不是已测吞吐。需要分别记录系统占槽、供应商已接收未结束、供应商报告排队/执行；无法观察上游真实执行时，不能把“已接收”写成“GPU 同时生成”。

基础方案最多验证单模型并发 8、系统混合并发 32。进一步寻找边界采用独立扩展阶段：

- 在隔离环境先用模拟供应商测试 32→64→128→170 活跃槽，以及 256/512 请求积压、恢复和公平性；不产生模型费用。模拟结果只证明调度与故障处理能力。
- 真实混合负载按 64→128→160 逐级扩展，每档至少两波，并按需延长稳态观察；仅这三档两波就会增加 704 条视频。必须重新估算余额和预算，并在低峰窗口执行，预留正常业务空间。
- 单模型真实阶梯可从 8→16→32 继续，Gemini 不超过当前共享池 20；再向上受 worker 总并发及供应商配额限制。首次出现限流即停升档，回退确认稳定区间。
- 当前单 worker 总并发 170，不能在该配置下证明单渠道支持 200。更高极限需隔离压测环境和已确认的供应商额度，不能靠直接提高生产配置验证。

生产本轮不执行杀 worker、断 Redis、断数据库等故障注入；这些在模拟环境验证。上线真实压力优先观察自然限流与失败恢复。

## 6. 观察指标与通过标准

每条记录完整时间线：请求提交→入队→领取→等待执行/渠道槽→供应商接收→供应商完成→下载/TOS/媒体探测→结果入库→页面观察到结果。缺少时间点先补采集，不能把队列等待归到模型耗时。

|维度|观察内容|建议判定|
|---|---|---|
|请求正确性|模型、时长、尺寸、比例、参考原图、模式|不应出现可提前校验的 4xx；出现立即停该用例|
|媒体可用性|实际可解码、时长、尺寸、完整性|逐条 ffprobe；时长初始容差 max(0.5 秒, 请求的 5%)，有供应商明确规范时按规范|
|视觉质量|角色身份、双人不串脸、动作/运镜、场景/风格遵循、闪烁变形|所有 A/B 样本人工查看；通过/部分通过/失败/无法确认，并保留时间点证据|
|音频|声轨存在、可听、语义与画面关系|只对要求且支持音频的样本判定；声轨存在不等于语义合格|
|系统正确性|丢单、重复提交/收费、跨用户数据、状态残留|均为 0；完成后侧栏“生成中”消失|
|可追溯性|Agent 标识、用量、供应商任务、费用|100% 可关联，不能混入普通业务统计|
|服务稳定性|API 延迟/5xx、worker 槽、队列长度与排空时间|与低负载基线对比；队列增长本身不能直接判系统异常|
|供应商能力|接受/完成吞吐、限流、错误、完成耗时|报告实际达到的并发及区间，不把未出现失败当成测到上限|
|资源|CPU、内存、I/O、网络、DB 连接、Redis、结果处理队列|连续采样，定位供应商生成和本地归档瓶颈|

A/B 每条至少检查开头、25%、50%、75%、结尾五个时间点，有动作异常时连续播放。C/D 全量自动检查、每模型每档抽查至少两条，并查看所有失败/异常耗时样本。

P50/P95 按模型、渠道、并发档和阶段分组；同一组少于 30 条时标明探索性样本并展示全部耗时/最大值，不能给出稳定 P95 或 SLA 结论。必要时在预算内补样。

## 7. 停止与恢复规则

- 任意跨用户数据、重复收费、漏记归因、任务丢失：停止所有新增提交，保留在途任务并核查。
- 首次 429 暂停该渠道升档；连续两次 429 或一个足够样本窗口错误率超过 5%，停止该渠道新提交，待冷却后回退档位复验。共享池按渠道联动停止。
- 非法参数、内容拒绝、余额不足不自动重复付费请求；保存真实错误分类，不能都展示成内容政策错误。
- 供应商已接受但状态不明时先对账，不重新创建第二个生成任务；传输重试复用同一幂等标识。
- CPU/内存持续超过 85% 两分钟，或 API P95 连续两个观察窗口超过低负载基线两倍且超过 1 秒，暂停升档并分析；出现正常业务影响时立即停止新压力流量。
- 预算按“已花费＋已提交在途预计费用”控制，到达阶段上限即停止新增请求。停止提交不代表上游可取消或可免单。
- 所有运行设最大观察期限；超时进入待核对列表，不无限轮询，也不自动无限重试。

## 8. 原提案费用估算（历史预算，已失效）

下表保留原提案合计，便于追溯当时范围，不是实际支出或当前可执行预算。原始合计包含已退役 PPIO/BFL，不能直接扣除两个模型后沿用其他压力波次。已移除退役渠道的逐秒配置报价；历史实际费用须查原工单、用量和供应商账单，不因退役归零。

保留模型当时的逐秒估价（人民币）：Wan 0.6、Kling 0.8、英和 H3 0.425、Gemini 0.72、Grok 0.137664。仅解释历史预算，不是现行费率；失败收费、分辨率/音频附加费以实际账单为准。

|原提案阶段|历史估算视频费用|
|---|---:|
|A 统一样本 14 条|¥94.13|
|B 业务样本 37 条|¥282.40|
|C 单模型阶梯 140 条|¥561.96|
|D 混合并发 64 条|¥266.16|
|基础合计 255 条|¥1,204.65|

当时提议的预算上限为 ¥1,600，未授权执行该基础方案。新测试须仅针对获准的保留模型重新估价和审批；大纲、参考图、稳态测试与扩展压力费用单独核算，不按本历史表放行。

## 9. 交付物

1. 测试清单 CSV/JSON：每条请求参数、参考 URL、提示词、阶段/并发档、工单及供应商 ID、所有时间点、状态、费用、输出 URL。
2. 可播放的质量报告：原参考图、原始提示词、输出视频、逐项质量结论及失败时间点；统一样本和业务样本分别展示。
3. 并发报告：按模型/渠道分组的吞吐、耗时、错误/限流、队列与系统资源曲线，明确最后稳定档、首个退化档及未测区间。
4. 缺陷清单：大纲合法性、错误映射、并发公平性、重复计费、结果归档、侧栏状态、刷新恢复等，附最小复现和优先级。
5. 上线建议：分别给出质量是否可用、系统建议并发、渠道建议并发；未核实真实能力的三个模型继续保留暂停状态。
