# 2026-09-23 服务器发布记录

## 发布结果

- 目标：既有服务器 `120.24.38.200`，项目目录 `/opt/mv-agent-frontend`。
- 运行版本：`git-0f74d25ebaa549e693b31230fecc0f4971d2104c`。
- MV 发布完成时间：`2026-09-23T07:55:21Z`。
- 前一 MV 版本：`git-06faad4c9dce612be90672bc307a001f7b03cb99`。
- MV 与独立模型服务六个镜像均在服务器从同一 Git 提交构建，分别通过各自 `scripts/deploy.sh` 发布；没有直接编辑服务器源码。
- MV Alembic：`e7a3b9c2f104`；独立服务 Alembic：`0006`。退役渠道无未删除的供应商/模型配置，历史兼容列保留；三个头肩照相关模板已发布迁移版本，历史保留。
- MV 前后端、五类 Worker、数据库、Redis，以及独立服务五个进程均通过健康检查。

## 备份与权限

发布前生成 `/var/lib/docker/mv-agent-maintenance/backups/mvagent-20260923T074056Z.dump` 和 SHA-256，并在临时数据库中完整恢复验证（37 个用户）。临时恢复库已清理，正式数据与备份保留。

独立服务首次建立 `model_service` 数据库、迁移/公开执行/管理三个角色。运行角色不是超级用户，无 schema CREATE、物理 DELETE 或 MV 业务表访问权限。三份运行/迁移环境文件均为 0600。供应商和业务签名凭据仅进入公开 API/Worker，没有复制到管理 API。

## 已验证功能与边界

- 国内英和、海外英和和 ToAPIs 的生产余额采集成功；商户余额与 Key 限额分别展示。
- 国内人民币；海外美元按 6.9 折算人民币；ToAPIs 按 1000 供应商积分 = 35 元展示。没有将未配置渠道显示为零余额。
- 独立管理后台「渠道余额」「模型供应商 → 登记验收」及用户门户通过真实浏览器检查。
- 独立目录包含 19 个模型、38 条路由及报价。路由继续关闭、等待验收；生成就绪检查明确返回 false。
- MV `MODEL_GATEWAY_URL` 继续为空，现有业务不切入上述待验收路由。MV 容器到独立模型 API 的内部网络连通性已验证。
- 本次上线未触发真实模型生成，未新增付费生成或覆盖线上系统头像。

## 访问方式

MV 保持原地址。独立服务维持服务器 loopback 监听，通过 SSH 转发访问：

```bash
ssh -N \
  -L 127.0.0.1:15180:127.0.0.1:5180 \
  -L 127.0.0.1:15181:127.0.0.1:5181 root@120.24.38.200
```

管理后台为 `http://127.0.0.1:15180`，用户门户为 `http://127.0.0.1:15181`。本次已在开发机建立对应隧道。管理员凭据另存在受 Git 忽略、权限 0600 的本机私密文件中，不写入本报告。

## 验证证据

- 完整 `make preflight` 通过：487 后端测试通过、1 个既有 xfail；228 前端测试；格式、lint、迁移、前端构建及本机 Docker 构建通过。
- 独立服务 `make check`：87 项测试、格式/lint、两个前端构建通过。
- MV 线上 API 契约/权限/隔离/软删除旅程：1 项通过；线上用户浏览器旅程：1 项通过。
- 管理 API：1 项通过；管理后台浏览器及内容管理员权限旅程：2 项通过。
- 管理后台旧验收脚本有重复标题和按钮文本导致严格定位冲突，已限定标题级别和供应商名称前缀，重跑通过。该测试修正与本发布记录作为后续非运行代码提交保留，不改变已验证的生产镜像版本，也不为测试定位变化重启生产。
- `scripts/online-health-check.sh` 全部通过；部署版本接口与运行镜像一致。

详细日志在本机 `/tmp/production-*-20260923.log`；独立后台截图与余额检查快照位于被 Git 忽略的 `output/production-release-20260923/`。凭据、数据库、构建产物与截图均不纳入提交。
