# 发布完善记录（2026-09-22）

对应上一轮审查提出的问题。本轮完成代码与发布工具完善，没有部署生产，没有提交或推送，也没有真实模型调用。

## 已完成

- **图片参数**：ToAPIs 普通图片请求按具体模型转换比例与分辨率，不再固定方形。GPT 2.5 普通版协议本身固定 high；明确指定不支持的画质会返回 422，而不是静默替换。报价和生成共用转换，报价返回 normalized_parameters；4K 的 2880×2880 别名不会再估为 2K。图片参考模式计费识别同时覆盖 image/images/image_urls/reference_images。
- **路由选择与验收**：保留上一轮的备用渠道凭据修复；报价选择已配置的已验收启用路由。运行时也拒绝未验收路由，Agent 专属验收入口除外。管理后台新增「模型供应商 → 登记验收」，要求已有成功 code-agent 工单、Agent/Test 批次、实际用量及一致的路由参数，再记录参数/产物/用量审核。登记不会发起生成，也不自动启用或发布收费规则。
- **首次部署复现**：新增 Alembic 0006 和无密钥目录/报价产物，空库安装 19 个模型、38 条关闭的路由及报价规则。已有配置和软删除记录不覆盖；旧 seed 发现统一目录后不再追加启用旧模型。报价同步不再自动读取开发 .env 或日期写死的 .runtime 目录，要求显式目标数据库和快照目录。
- **生成就绪校验**：release-readiness.py 分开报告目录已安装与生成是否就绪；无启用路由、缺少验收、凭据或正式收费/预占规则时返回非零。明确的 --catalog-only 模式仅允许目录/报价阶段通过，不伪装生成已就绪。
- **Worker 停机**：SIGTERM/SIGINT 停止领取，默认排空 105 秒，停止余额采集。取消提交结果不确定的任务转人工核账；已确认任务重新排队恢复查询或归档，不重复生成。
- **头肩照模板升级**：新增 MV Alembic e7a3b9c2f104，以冻结的已知历史模板替换规则，保留运营追加内容、旧正文及草稿，版本取全部已有版本最大值加一。兼容仓库早期双图模板及旧元数据；不认识的自定义基础正文或安全约束冲突直接停止，防止覆盖。已在本机 PostgreSQL 迁移检查通过。
- **系统头像迁移工具**：prepare-headshot-migration.py 从完整 prepared 映射生成 Alembic 文件和匹配的 headshot-assets.json 种子文件。执行迁移时再次校验系统范围、私有用户隔离及完整旧值，拒绝过期数据；重放同批次不写入。新库使用同一套原图、缩略图、英和 asset:// 和身份文案；启动不再覆盖已版本化头像的身份信息。
- **Docker 上下文隔离**：根 .dockerignore 排除整个独立服务目录及递归 .env/.secrets/.runtime、生成输出目录。独立服务使用各自构建上下文，不能将本机凭据和媒体带入 MV 前端构建。发现问题后已取消原 MV 构建，并重新执行隔离后的构建；仅定向清理了本轮旧构建产生的源上下文及 COPY 缓存。

## 保持明确的运行状态

首次目录中的路由继续关闭、verification=pending，正式费率未由估价自动生成。这是保留原配置与已有验收约束，并非将不完整配置伪装成可用：历史生成成功只能证明一次任务成功，不自动证明规格、产物和收费均已核实。现在有管理入口和只读检查工具完成后续发布流程，无需直接修改数据库。

系统头像的真实重生成、审核和线上资产替换不包含在此次代码完善中；工具链不伪造新图片或未经审核的 TOS 地址。新迁移、数据产物和代码需一起版本化构建后发布。

## 验证

- 独立服务：87 项测试，两个前端构建；后续针对验收证据校验及就绪检查的 5 项测试通过。
- MV：487 项后端测试通过、1 项既有 xfail，覆盖率 69.77%；前端 228 项通过。
- 浏览器：主项目 6 项；独立门户的注册、Key/额度、账单、报价配置及不存在工单不能登记验收通过。
- 独立 PostgreSQL 空库：0001–0006 完整迁移、seed 重放，19 模型 / 38 路由 / 未自动启用，通过；验收完成删除的仅是本轮临时测试数据库。
- 资产迁移测试：旧值冲突、私有人物拦截、成功写入及重放无写入；模板升级测试覆盖附加运营规则与草稿保留。
- 格式、lint、git diff --check 通过。make preflight 的检查、迁移、测试及前端构建通过；其 Docker 阶段为修复上下文排除规则主动中止，随后单独重跑 make docker-build 成功。MV 两个镜像和独立服务四个镜像均构建成功。

本轮日志在 /tmp/fix-mv-preflight.log、/tmp/fix-mv-docker-clean.log、/tmp/fix-service-check.log、/tmp/fix-service-docker.log、/tmp/fix-mv-browser.log、/tmp/fix-portal-browser.log。日志、私密配置、数据库和生成产物均不纳入提交。
