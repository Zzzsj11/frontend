# 前端自动化测试注意事项

> 写给接手的前端自动化测试开发。本文档列出本次迭代新增的前端功能点及测试时需要注意的地方。

## 测试环境

- 本地：`http://localhost:5173`（nginx 代理 → backend:8000）
- 线上：`http://124.222.219.76:5173`
- 测试账号：dev01 / supermv007

## 需要覆盖的前端功能点

### 1. 确认弹窗 Enter/Escape

- 操作：删除项目/子项目/分镜行 → 弹出确认框
- 验证：按 Enter 确认删除，按 Escape 取消
- 注意：弹窗是 Teleport 到 body 的，需用 `page.locator('[role="alertdialog"]')` 定位

### 2. 侧边栏选中状态持久化

- 操作：选中某个子项目 → 刷新页面
- 验证：刷新后自动恢复到之前选中的子项目
- 注意：依赖 localStorage，需在同一浏览器上下文中测试

### 3. 侧边栏拖拽排序

- 操作：拖拽歌曲项目或子项目到新位置
- 验证：松手后顺序更新，刷新后顺序保持
- 注意：HTML5 drag & drop，Playwright 用 `page.dragAndDrop()` 或 `locator.dragTo()`

### 4. 大纲弹窗 Tab 切换

- 操作：打开 MV 大纲弹窗（需先创建 ASS 项目并生成大纲）
- 验证："场景总览"和"逐镜大纲"两个 tab 可切换
- 注意：需要后端真实生成大纲数据，或用 mock

### 5. 数字人资产库 - 卡片点击

- 操作：打开资产库 → 点击系统角色头像 → 点击左下角"查看"按钮
- 验证：头像点击显示全屏大图（Escape 关闭）
- 验证："查看"按钮打开编辑弹窗
- 验证：点击卡片加入/移出阵容

### 6. 数字人上传两阶段

- 操作：打开上传面板 → 输入名称/风格 → 上传图片 → 点击"添加到资产库"
- 验证：按钮文案变化 "正在上传…" → "正在生成三视图…" → 完成后清空
- 验证：生成期间所有输入框 disabled，不可修改
- 注意：两阶段切换很快（上传通常 < 1s），可能需要 mock 或等待

### 7. 编辑弹窗角色锁定

- 操作：ASS 项目 → 行卡片 → 编辑视频内容 → 人物 tab
- 验证：角色卡片显示选中效果但不可点击（disabled + 提示文字）
- 注意：通用分镜不受此限制，需分别验证

### 8. 视频生成后下载按钮

- 操作：生成视频后查看播放器右下角
- 验证：导出按钮旁出现下载按钮，导出完成的压缩包

### 9. 通用分镜默认值

- 操作：打开通用分镜弹窗
- 验证：空镜=1、人物镜=1、总时长=8s、分辨率=480p

### 10. 侧边栏宽度拖拽

- 操作：拖拽侧边栏右边缘
- 验证：最大宽度约 392px（原 232px × 1.69）

### 11. 403 自动跳转登录

- 操作：用过期 token 发请求
- 验证：弹窗"登录已过期"→ 页面跳转到 /login
- 注意：需 mock 或修改 token 触发

## 不适合自动化的点

| 功能             | 原因                          |
| ---------------- | ----------------------------- |
| 图片生成质量     | 需要视觉判断                  |
| 拖拽排序动画     | CSS 动效，Playwright 难以验证 |
| 卡片压扁效果修复 | 视觉问题，需人工确认          |
| 数字人生成进度   | 依赖真实 AI 服务，耗时长      |

## 推荐测试脚本结构

```
e2e/
  test-sidebar-persistence.spec.ts   # 侧边栏选中恢复
  test-sidebar-drag.spec.ts          # 拖拽排序
  test-outline-tabs.spec.ts          # 大纲 Tab
  test-dh-library.spec.ts            # 数字人资产库
  test-confirm-dialog.spec.ts        # 确认弹窗 Enter
  test-export.spec.ts                # 导出/下载按钮
  test-cast-locked.spec.ts           # 角色锁定
```
