# Project TODO List

## 生成模型扩展

- [ ] 当新的图片或视频模型可正式接入时，完成 [`docs/TODO_MODEL_EXPANSION.md`](docs/TODO_MODEL_EXPANSION.md) 中的全部接入清单。
- [ ] 不要只给前端下拉框增加名称；必须同步完成后端模型白名单、能力矩阵、供应商参数映射、任务配置持久化、Token/费用账单、旧任务兼容和自动化测试。
- [ ] 模型数量超过一个后，将当前硬编码选项改为后端模型能力接口，并根据模型动态联动画幅、清晰度、时长和参考图约束。

当前固定模型：图片 `Img2`（`gpt-image-2`），视频 `sd2.0`（`doubao-seedance-2.0`）。ASS 与通用分镜界面中的模型选择器已预留，但在只有一个可用模型时保持禁用。
