# 远程 e2e 凭据（复制本文件为 e2e/.env 后填写；e2e/.env 已 gitignore，禁止提交）
# 打非 localhost 目标时 REMOTE_E2E_PASSWORD 必填，否则 e2e/env.ts 会拒绝启动
REMOTE_E2E_USERNAME=admin
REMOTE_E2E_PASSWORD=
# 可选：固定测试批次 ID（默认自动生成时间戳），同时用作 X-Test-Run-Id 与产物目录名
# REMOTE_E2E_RUN_ID=
