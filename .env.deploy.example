# Copy to .env.staging or .env.production on the target server. Never commit real values.
# TCR: ccr.ccs.tencentyun.com/your-tcr-namespace
# No-registry server-local release: mvagent-local
REGISTRY=mvagent-local
JWT_SECRET=replace-with-at-least-32-random-characters
POSTGRES_DB=mvagent
POSTGRES_USER=mvagent
POSTGRES_PASSWORD=replace-with-strong-random-password
FRONTEND_PORT=5173
BACKEND_PORT=8000
